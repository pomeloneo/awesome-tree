import { createAwesomeTree } from '../src'
import { treeData } from './tree-data'

describe('create-awesome-tree', () => {
  const awesomeTree = createAwesomeTree(treeData)

  it("test awesomeTree's return", () => {
    
  })
})
